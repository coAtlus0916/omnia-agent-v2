# omnia.create-associate

Version 0.2.104, sequence 106, is the current source boundary prepared for isolated signing. It retains the exact GRA identity, durable closure, receipt equality, and DCNO inheritance/dependency contracts from immutable 0.2.103, while declaring only the Feature-specific Python bridge/entry paths and the exact 34 Store ports its Worker and bridge call. This source requires Shell 0.4.15, is not installed, and does not claim a live Omnia Return canary.

After workbook validation succeeds, recording coverage, `returnSupport`, and live-canary history are evidence metadata rather than Return permissions. `Oracle EBS`, OS `AD`, and `代码迁移工具` therefore reach the same parameterized authority preflight as the recorded values; Oracle EBS uses its declared exact Omnia content name `Oracle eBusiness Suite`, while values without a declared remote alias are never guessed. Zero or multiple authoritative identities, safety/authority mismatch, invalid or contradictory RAIT, API rejection, remote drift, and uncertain prior mutation still fail closed.

The managed V4 upload recognizes an explicit fifth local input kind, DCNO. Review shows `DCNO ID`, `DCNO 类型`, `Omnia工作区` and `关联系统ID`; Python requires type `网络` and at least one APP reference. A target may be another APP in the upload or one exact active Application already present in the current Pack. Cross-Workspace and off-batch references are warnings after signed live identity/GRA/RAIT verification; missing, ambiguous, inactive or non-Application targets remain blocking. Multiple valid APP sources derive Higher when any source is Higher and derive Lower only when all are Lower. The current immutable recording is Artifact `110eba6d-dd39-4b20-bfd5-83caefd20260`, recordingId `8aa3673e-53b7-4902-bca6-7b86d5cc62be`, with 992 events. It records `Infrastructure`/`Network`, GRA contentId `60241274` (`通用网络设备`), `InfrastructureApplication`/`ConcurrencyTabId=602`, and `any_higher_else_all_lower` RAIT inheritance. DCNO does not run APP-only `IT风险评估`/Factors or settings/scoring/AI review, but does run the parameterized GRA/Risk-Control/Evaluation stages. Higher and Lower share the same exact 3-Risk/8-Control catalog; the mother workbook selects 3 Higher links and 2 Lower links, and zero-link Risk `RAITCOR001` becomes `ClassificationNA` in both modes. The recording/read-only evidence and targeted offline checks do not claim a live Return canary.

DB/OS inheritance remains deterministic for one or many APP sources. A structured decision records every source mode, the `any_higher_else_all_lower` policy and its result; valid mixed sources pass and resolve Higher, while missing or invalid sources remain blocking.

The APP type selector is a projection of the signed mother-workbook declaration, not a hard-coded product list. `SAP S/4 HANA` uses the common 15-item APP scoring capability and its own exact `REL.APP.SAP_S4_HANA.*` Risk-Control family. The complete 1587-event frozen stream proves the shared 30-Control catalog across native `SAPS4`/`SAPCUA`/`SAPCHARM`/`IMP` numbers; the mother workbook selects 18 Higher links and 17 Lower links without ordinal or translated-description guessing. A separate Lower recording is evidence-only and is never a Return permission.

Version 0.2.54, sequence 56. Tool exposes a real required APP relation field and uses the existing signed `ItToolApplication` contract. DB/OS may reference multiple exact APP rows; Python freezes all sources and derives Higher if any is Higher, otherwise Lower only when all are Lower. Settings and Risk-Control remain row-pipelined at concurrency three, maximum four. A real confirmed toggle can isolate one failing row and its dependants while independent rows continue; partial work remains partial, uncertain mutation is not replayed, and every same-row signed preflight/mutation/read-back chain remains serial.

The V3 user APP schema contains only 系统ID, APP类型, System Risk Classification, Factors Considered, and Omnia工作区. `isDataAvailable` is not user input: new Applications use the signed false default, while existing Applications preserve the authoritative live value. Candidate rows are selected from OOXML input structure: data-validation row coverage, populated input-border cells, or multiple populated declared columns. Borderless or merged section/title/description bands are not data merely because their label occupies the preceding section's identity column. A structurally eligible partial row is retained with source location and receives a blocking template/required-field issue when its identity is empty or fewer than two declared columns are populated. Review shows exactly 11 canonical checks; AI and live checks are never reported passed unless actually executed. Missing Connector binding or an empty Workspace safety scope fails every dependent live check with the exact missing prerequisite. Once both are available, revalidate/apply continue through the existing signed Connector Operations for APP identity/recycle, non-APP active identity, relationship and Workspace evidence. Relationship targets are described as resolvable—not falsely as already existing—and both relationship and inherited-RAIT checks consume the referenced APP's live identity, Application type, active status, Workspace and RAIT result. Revisions are CAS-bound and trigger a full revalidation. Removing a row persists an exclusion only, never calls a mutation Operation, and never deletes an Omnia object.

Factors Considered review uses the Shell-owned `ai.review` port and the currently configured, successfully tested Provider. The Worker sends only bounded structured APP fields and receives no Provider URL, model configuration, or secret. A real, strictly validated response with exact row coverage records `reviewId`, usage and capture time; no concerns is passed, suggestions are warning-only, and unavailable, timed-out or invalid Provider results remain `not_evaluable` warnings. User-facing `summary`, `message` and non-empty `suggestion` values must satisfy the versioned `zh-CN-simplified/v1` contract. Obvious English-only output is rejected by the Worker and can be retried; it is neither translated in the Renderer nor stored as a successful cache entry. AI never changes a field, authorizes Return, or creates an Omnia effect.

The durable Return control loop uses exact APP and generic DB/OS/Tool identity resolvers. Diagnostic reads grant no mutation permit. Review and Return preparation treat an active same-name object as a blocking conflict, not as implicit reuse. The sole exception is an object whose exact object ID, Workspace, external identity and exact object type have one committed Agent-managed create proof under the current Connector session, Authority, tenant/org, Pack and Engagement. APP proves `Application`, DB/OS prove `Infrastructure` after the generic live resolver independently proves their exact subtype, and Tool proves `ITTool`; an unowned or type-drifted match remains blocking. An owned generic active match freezes `resume`, its resolved object ID and its proof identity, and execution re-proves the same record before read-only closure. The second create-only preflight repeats the authoritative query for the exact object type and grants a one-time permit only for an unambiguous absent identity. Off-batch, duplicate, ambiguous and cross-Workspace APP references remain blocked; DB/OS may retain multiple distinct valid APP sources, while Tool requires exactly one. No target Omnia/Pack run has been performed.

见 PRODUCT/TECHNICAL/CONTRACT/TESTING/OPERATIONS/VERSION 文档。本版本不声称真实 Omnia canary 已通过。
