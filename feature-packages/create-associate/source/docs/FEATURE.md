# omnia.create-associate

Version 0.2.49, sequence 51. Return confirmation/continuation awaits the serial Return executor inside the same authorized `omnia_mutation` Worker invocation that approved the frozen intent. Each verified command also produces a disposable Core-backed Surface projection, so total and capsule progress update during the long invocation without a second Worker call or fabricated timer. Generated Risk identities settle first; each unique Risk is classified through a signed PATCH that uses only Risk-owned concurrency evidence when present, followed by exact readback. Only then may the complete Risk-Control relation multiset settle and associate. Cross-Run recovery accepts APP/DB/OS/Tool only with exact current-authority managed-create proof; ordinary same-name objects remain blocking.

The V3 user APP schema contains only 系统ID, APP类型, System Risk Classification, Factors Considered, and Omnia工作区. `isDataAvailable` is not user input: new Applications use the signed false default, while existing Applications preserve the authoritative live value. Candidate rows are selected from OOXML input structure: data-validation row coverage, populated input-border cells, or multiple populated declared columns. Borderless or merged section/title/description bands are not data merely because their label occupies the preceding section's identity column. A structurally eligible partial row is retained with source location and receives a blocking template/required-field issue when its identity is empty or fewer than two declared columns are populated. Review shows exactly 11 canonical checks; AI and live checks are never reported passed unless actually executed. Missing Connector binding or an empty Workspace safety scope fails every dependent live check with the exact missing prerequisite. Once both are available, revalidate/apply continue through the existing signed Connector Operations for APP identity/recycle, non-APP active identity, relationship and Workspace evidence. Relationship targets are described as resolvable—not falsely as already existing—and both relationship and inherited-RAIT checks consume the referenced APP's live identity, Application type, active status, Workspace and RAIT result. Revisions are CAS-bound and trigger a full revalidation. Removing a row persists an exclusion only, never calls a mutation Operation, and never deletes an Omnia object.

Factors Considered review uses the Shell-owned `ai.review` port and the currently configured, successfully tested Provider. The Worker sends only bounded structured APP fields and receives no Provider URL, model configuration, or secret. A real, strictly validated response with exact row coverage records `reviewId`, usage and capture time; no concerns is passed, suggestions are warning-only, and unavailable, timed-out or invalid Provider results remain `not_evaluable` warnings. AI never changes a field, authorizes Return, or creates an Omnia effect.

The durable Return control loop uses exact APP and generic DB/OS/Tool identity resolvers. Diagnostic reads grant no mutation permit. Review and Return preparation treat an active same-name object as a blocking conflict, not as implicit reuse. The sole exception is an object whose exact object ID, Workspace, external identity and exact object type have one committed Agent-managed create proof under the current Connector session, Authority, tenant/org, Pack and Engagement. APP proves `Application`, DB/OS prove `Infrastructure` after the generic live resolver independently proves their exact subtype, and Tool proves `ITTool`; an unowned or type-drifted match remains blocking. An owned generic active match freezes `resume`, its resolved object ID and its proof identity, and execution re-proves the same record before read-only closure. The second create-only preflight repeats the authoritative query for the exact object type and grants a one-time permit only for an unambiguous absent identity. Off-batch APP, multiple APP and cross-Workspace inheritance remain blocked. No target Omnia/Pack run has been performed.

见 PRODUCT/TECHNICAL/CONTRACT/TESTING/OPERATIONS/VERSION 文档。本版本不声称真实 Omnia canary 已通过。
