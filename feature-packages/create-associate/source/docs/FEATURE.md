# omnia.create-associate

Version 0.2.22, sequence 24. The Feature exposes four real workflow phases: 上传资料、校验、Comments 复核、回传. Comments and Return remain compact projections over the complete immutable Core plan and real intent/command evidence. Application settings follow the v4 two-stage type/relevance then data-availability protocol inside one signed mutation Operation, with fresh 501 concurrency evidence obtained only from the intervening live GET. GRA object intents freeze their real canonical identity (`GRA-${elementId}`) as `externalId`; the parent IT Element identity remains linked only through `entityObjectTargetKey`, so preview, create/reuse command validation and signed readback all use the same GRA name. GRA detail readback accepts only the real Omnia risk-scope shapes: a bare array or exactly one `$values`/`results`/`items`/`value` array envelope. It resolves one active entity GUID without recursive guessing and rejects absence or conflict. An exact existing APP GRA with empty live RAIT is an explicit incomplete reuse candidate; its resolved RAIT remains the planned value and the normal GRA-state command must still PATCH and read back that value. A non-empty conflicting live RAIT remains blocked. New `create` objects use no-token bootstrap; `resume|reuse` requires a live token unless Core proves the exact object was created by this Feature's earlier committed object-create command under the same authority/Pack/engagement/Workspace and the current signed read still shows the exact empty settings state. That narrowly proven recovery uses `recover_owned_create_bootstrap`; external objects never receive no-token bootstrap. The signed package carries the exact `Phase1-用户填写模板V3.xlsx`; APP/DB/OS/Tool validation, authoritative Workspace mapping, optional tenant semantics, safety scope, confirmation, mutation permits, and receipt-backed readback remain unchanged.

The V3 user APP schema contains only 系统ID, APP类型, System Risk Classification, Factors Considered, and Omnia工作区. `isDataAvailable` is not user input: new Applications use the signed false default, while existing Applications preserve the authoritative live value. Review shows exactly 11 canonical checks; AI and live checks are never reported passed unless actually executed. Missing Connector binding or an empty Workspace safety scope makes all three live checks failed with the exact missing prerequisite. Once both are available, revalidate/apply continue through the existing signed Connector Operations for APP identity/recycle, non-APP active identity, relationship and Workspace evidence. Revisions are CAS-bound and trigger a full revalidation. Removing a row persists an exclusion only, never calls a mutation Operation, and never deletes an Omnia object.

The durable Return control loop uses exact APP and generic DB/OS/Tool identity resolvers. Diagnostic reads grant no mutation permit; the second create-only preflight repeats the authoritative query for the exact object type and grants a one-time permit only for an unambiguous absent identity. Off-batch APP, multiple APP and cross-Workspace inheritance remain blocked. No target Omnia/Pack run has been performed.

见 PRODUCT/TECHNICAL/CONTRACT/TESTING/OPERATIONS/VERSION 文档。本版本不声称真实 Omnia canary 已通过。
