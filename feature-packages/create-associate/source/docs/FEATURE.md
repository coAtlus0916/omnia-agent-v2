# omnia.create-associate

Version 0.2.44, sequence 46. Return confirmation/continuation awaits the serial Return executor inside the same authorized `omnia_mutation` Worker invocation that approved the frozen intent. Core therefore receives the original mutation action identity for every command; execution never continues after the action result has cleared that authority. Each verified target still persists receipt-backed progress and its checkpoint before the next target. Generated Risk/Control settlement remains bounded to 120 seconds and 40 reads. Cross-Run reservations remain based on Workspace-bound business identities, and workbook parsing remains fail-closed.

The V3 user APP schema contains only 系统ID, APP类型, System Risk Classification, Factors Considered, and Omnia工作区. `isDataAvailable` is not user input: new Applications use the signed false default, while existing Applications preserve the authoritative live value. A recognized section row is skipped only when every cell is blank; any row containing user content is retained with source location and receives a blocking template/required-field issue when its identity is empty or fewer than two declared columns are populated. Review shows exactly 11 canonical checks; AI and live checks are never reported passed unless actually executed. Missing Connector binding or an empty Workspace safety scope fails every dependent live check with the exact missing prerequisite. Once both are available, revalidate/apply continue through the existing signed Connector Operations for APP identity/recycle, non-APP active identity, relationship and Workspace evidence. Relationship targets are described as resolvable—not falsely as already existing—and both relationship and inherited-RAIT checks consume the referenced APP's live identity, Application type, active status, Workspace and RAIT result. Revisions are CAS-bound and trigger a full revalidation. Removing a row persists an exclusion only, never calls a mutation Operation, and never deletes an Omnia object.

The durable Return control loop uses exact APP and generic DB/OS/Tool identity resolvers. Diagnostic reads grant no mutation permit. Review and Return preparation treat an active same-name object as a blocking conflict, not as implicit reuse. The sole exception is an APP whose exact object ID, Workspace and external identity have one committed Agent-managed create proof under the current Connector session, Authority, tenant/org, Pack and Engagement; DB/OS/Tool active matches remain blocked because no equivalent owned-recovery contract is released for those kinds. The second create-only preflight repeats the authoritative query for the exact object type and grants a one-time permit only for an unambiguous absent identity. Off-batch APP, multiple APP and cross-Workspace inheritance remain blocked. No target Omnia/Pack run has been performed.

见 PRODUCT/TECHNICAL/CONTRACT/TESTING/OPERATIONS/VERSION 文档。本版本不声称真实 Omnia canary 已通过。
