# omnia.create-associate

Version 0.2.16, sequence 18. The Feature exposes exactly three user steps: 上传资料、校验、回传. Picker and drag/drop first create a durable source Artifact and an `acquiring` Run, then remain on Upload with the real filename and size. Only explicit 确认上传 atomically advances that Run to `processing`; the Surface immediately renders step 2 with a real 0/11 running projection, and a hidden declarative background action performs parsing, compilation, live validation and persistence after that render. The signed package carries the exact `Phase1-用户填写模板V3.xlsx`; Main verifies its member digest before Save As. APP/DB/OS/Tool sections are independently optional; every non-empty row still receives its own required/enum/duplicate/relation validation. 重新开始 also cancels an `acquiring` Run without deleting audit history. Review does not project the internal TemplateInstance as a downloadable artifact, and source Artifacts are never exportable. The authority Operation uses the verified `facets/byEngagementIds` Workspace response plus the live `Standardized Accounts List` content publication. GRA content resolution carries APP/DB/OS/TOOL kind and subtype explicitly, takes content/category keys from the current Engagement, and uses protocol type IDs 3/4/5. Review live validation and Return preparation both send the exact non-APP subtype. Optional `tenantOrOrgId` is frozen exactly, including the empty string, while authority instance, Pack, and Engagement remain required. Catalog query values are declared parameters and encoded by Operation Host. Omnia/Core GUID validation accepts any non-zero canonical 8-4-4-4-12 hexadecimal identity and does not impose RFC UUID version or variant bits. APP identity resolution, action-time APP create preflight, IT Element read-back, and Application-settings reads derive Workspace only from the exact Work Item Facet Mapping and reject missing, multiple, or drifted mappings.

The V3 user APP schema contains only 系统ID, APP类型, System Risk Classification, Factors Considered, and Omnia工作区. `isDataAvailable` is not user input: new Applications use the signed false default, while existing Applications preserve the authoritative live value. Review shows exactly 11 canonical checks; AI and live checks are never reported passed unless actually executed. Missing Connector binding or an empty Workspace safety scope makes all three live checks failed with the exact missing prerequisite. Once both are available, revalidate/apply continue through the existing signed Connector Operations for APP identity/recycle, non-APP active identity, relationship and Workspace evidence. Revisions are CAS-bound and trigger a full revalidation. Removing a row persists an exclusion only, never calls a mutation Operation, and never deletes an Omnia object.

The durable Return control loop uses exact APP and generic DB/OS/Tool identity resolvers. Diagnostic reads grant no mutation permit; the second create-only preflight repeats the authoritative query for the exact object type and grants a one-time permit only for an unambiguous absent identity. Off-batch APP, multiple APP and cross-Workspace inheritance remain blocked. No target Omnia/Pack run has been performed.

见 PRODUCT/TECHNICAL/CONTRACT/TESTING/OPERATIONS/VERSION 文档。本版本不声称真实 Omnia canary 已通过。
