# Four-plane map

Surface 只采集/展示；Worker 解析、验证、编译计划；Core Store 持久化；Connector 只托管签名 Operation。
# Implementation map

Version 0.2.104 / sequence 106 keeps semantic content in the Feature/Core boundary: Worker freezes `contentName`, authority `inkContentId/typeId`, and exact GRA disposition; reuse/closure paths freeze a no-mutation command spec before readback; Core compares the projected payload to the exact receipt response. The live verifier rejects extra dependency relations and independently proves every DCNO inheritance-source readback. The isolated runtime uses only `middle/create-associate-python-bridge.cjs`, `python/create-associate-engine.py`, and the 34 Store ports declared from real source calls. DB/OS/DCNO dependencies require `InfrastructureApplication`, TOOL requires `ItToolApplication`, and Connector remains generic transport/session/gate/Operation hosting with no Feature business branch.

Version 0.2.98 / sequence 100 wires the existing Worker fresh-start path to a signed Shell lifecycle declaration. Surface declares one hidden `fresh-start-on-reopen` local-state action. Shell owns exact retained-instance `closed → open` detection and single-flight dispatch; Package Manager validates and freezes the lifecycle identity; Worker owns the Run/Return decision and exact-revision CAS; Core retains event, command, receipt and evidence authority. Renderer refreshes from Core only when the reopen action advances Surface revision. Minimum Shell version is 0.4.15. Connector and Bridge remain unchanged.

The exact V5 workbook is packaged as the download asset and feeds one signed registry into Python upload parsing, Review, frozen plan compilation, and Worker authority binding. Python no longer emits recording/support-matrix blockers, and `blockedPendingRecording` is always false. Worker no longer converts `returnSupport`, dedicated-recording coverage, or live-canary history into Review/live-validation/prepare-Return permissions. Exact content identity still comes from the signed Feature Operation at runtime; Oracle EBS's declared `Oracle eBusiness Suite` alias is parameter data, not a product switch. Connector Core remains transport/session/gate/Operation host only.

The immutable DCNO evidence is Artifact `110eba6d-dd39-4b20-bfd5-83caefd20260`, recordingId `8aa3673e-53b7-4902-bca6-7b86d5cc62be`, 992 events. Only the Feature's signed Operation adds the Network parameter and reuses the DB/OS parameterized Infrastructure engine; Connector carries Operation/transport and no Feature business logic. The Higher catalog is 3 Risk/8 Control with six exact enabled relations (`RAITCOR008→DCNO.05/.21/.22/.23/.24`, `RAITCOR006→DCNO.10`); observed `APP.03`/`APP.06` under `RAITCOR001` stay disabled. Recording/read-only catalog evidence and targeted offline checks are not a live Return canary.

Version 0.2.68 keeps category, scoring-governance selection and APP relation-family selection in the Feature package. Worker creates one APP-only category intent, executes it between GRA state and scoring, and compiles scoring/Control intents from the exact current APP content family plus RAIT in the signed mother workbook. The signed Operation resolves the exact category from the current GRA Risk Factor directory, owns fixed category and factor routes plus concurrency/read-back checks, and returns only verified state. Core continues to own immutable intent, command, receipt, evidence, progress and uncertain state; Connector Core remains transport/session/gate/Operation host only. Surface gains no new control.

Version 0.2.54 keeps the four-plane boundary. Python owns workbook normalization and the multi-APP any-Higher inheritance IR. Worker freezes execution policy, validates dependency identity and schedules unaffected rows after an isolated failure. Core continues to own immutable commands, receipts and projections. Surface's toggle is a generic typed action input whose boolean is frozen by Worker and echoed authoritatively. Operation owns the exact Settings token fast path and Risk-Control read-back shapes; Connector Core and route inventory are unchanged.

- Frontend: `frontend/surface.json` plus generic two-column workflow renderer, native picker/drop staging, explicit confirmation, post-render background action dispatch, signed-template Save As, artifacts, progress/issues, and issue editors.
- Python: `python/create-associate-engine.py` owns XLSX parsing and deterministic local validation; `python/plan_ir.py` compiles the registry-driven capability/dependency plan without I/O or Connector requests.
- Worker: `middle/worker.cjs` owns Run state, Remote-plan enrichment, dependency scheduling, revisions and orchestration; it does not maintain a second active XLSX parser, local-validation engine or kind-plan compiler.
- Backend: migration plus Core structured Run/artifact/template/provenance/issue/intent/command/evidence registries.
- Connector capability: `connector-capability/operation.ofop`; Connector Core supplies only signed route transport, session binding, gates, and Operation hosting.
- Machine-readable mapping: `contracts/implementation-map.json`; runtime I/O/event/error/Store ports are in `contracts/feature-runtime.json`, and signed test inventory/vectors are under `tests/`.
