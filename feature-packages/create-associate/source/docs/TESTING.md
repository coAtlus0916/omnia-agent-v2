# 测试

The source inventory covers the real signed V3 copy without a user `isDataAvailable` column, canonical revisioned `GRA-` naming, exactly 11 check identities, parser-issue preservation, every-blocker check mapping, field limits/legal names, exact-one/same-Workspace inheritance, external-APP fail-closed state, four-kind create/reuse paths, exclusion revalidation, Run-scoped issue IDs, and atomic Core CAS revision commits.

All 15 non-empty APP/DB/OS/Tool combinations are parseable and the four object kinds have real Return orchestration. New APP freezes signed `isDataAvailable=false`; a pre-existing APP preserves only its authoritative boolean. The execution contract requires bounded identity pagination, no permit from diagnostic reads, an object-type-aware create-only permit, relation-before-GRA for DB/OS, live APP GRA RAIT inheritance, read-back for every mutation, and read-only uncertain reconciliation. These source expectations are not a claim that a real Pack canary passed.

LibreOffice render evidence is a separate visual QA artifact. No automated fixture counts as a real Omnia canary.

The package carries declared test inventory and vectors, not a fabricated pass result. Actual command results belong to the repository acceptance record. Automated Connector harness results are not a real Omnia canary.
