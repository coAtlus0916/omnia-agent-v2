# 测试

The 0.2.104 / sequence 106 source self-check contains 34 independent fixtures and runs only with release CPython 3.13.14. Root regressions exercise all ten APP/DB/OS/TOOL/DCNO Higher/Lower planner modes, the S/4 catalog boundary, exact Oracle EBS alias/key/category and 11/7 catalog split, exact GRA content identity and disposition, receipt-bound base-GRA and later field projections, durable reuse/closure specs, DCNO inheritance-source evidence, exact dependency sets, and Delete final-plan derivation. The eight frozen workbooks cover 30 unique mode/value rows; the live verifier reports `pending` until same-version Core Runs, receipts, read-backs and final Delete absence evidence really exist. No offline fixture is a live canary.

The V5 Oracle EBS/AD/代码迁移工具 cases must have no `LOCAL/PLAN.CONTENT_PENDING_RECORDING`, must be `ready_for_remote_preflight`, and must freeze `blockedPendingRecording=false`. Worker source-contract coverage rejects any `returnSupport`/recording/live-canary permission gate and verifies the exact Oracle EBS authority alias without guessing AD. Existing authority/safety, identity, RAIT, drift and uncertain-state guards remain covered by the established suites. These are offline/source checks and do not claim a live Return canary.

The targeted S/4/DCNO regression continues to require shared Higher/Lower catalog inventory, S/4 18/17 links, DCNO 3/2 links, and explicit `ClassificationNA` for every zero-link Risk. Higher-only recording evidence never invalidates Lower. The source evidence does not claim a live Return canary.

The post-R7 regression replaces the prior self-derived 32-Control fixture. It uses explicit real response shapes (`SAPCUA.06`, an unrelated same-ordinal `IMP.06`, and a localized Draft.js JSON name) and proves that only the signed `catalogControlNumber` resolves. A required relation with no exact identity evidence must remain unmatched and be reported by `catalogIdentityEvidenceGaps`; response reordering must not change the catalog fingerprint, while adding a live identity must change it. The managed S/4 registry must be completely populated or explicitly empty/blocked; partial mappings are rejected during packaging. This is source evidence only, not a completed live S/4 mutation canary.

The former 0.2.84 “live-shaped” fixture was not real Pack evidence: it constructed every Control name from the same governance relation it was testing and merely padded the list to 32. It therefore could not detect localized Draft.js names or the actual `SAPCHARM`/`SAPCUA` families. Its historical candidate claim is retained below only as superseded evidence and must not be used as an acceptance result.

The 0.2.87 regression exercises arbitrary structured Control-family prefixes rather than a product-name allowlist. The Operation must retain the normalized live family and ordinal; a mismatched governed family may resolve only through the same ordinal plus one exact governed description inside the exact GRA. An unprovable display value uses only the exact-description path. Duplicate description and duplicate family/ordinal/description fixtures must remain unresolved and fail closed. A diagnostic fixture proves credential- and GUID-shaped text is redacted and no object ID field is projected.

The 0.2.85 regression exercises the signed catalog boundary with decorated SAP S/4 HANA numbers, full-width punctuation, a space-delimited SAP family number, and an unprovable non-canonical display value. Provable values must normalize to `SAP.<two-digit ordinal>`; the unprovable value must emit no number and resolve only through one exact governed description. A duplicate exact description must remain unresolved and fail-closed. Diagnostics must retain only a bounded `controlNumber`/`name` sample.

The 0.2.84 regression adds the observed numberless Control shape: one exact GRA returns six Risks and 32 Controls with each Control limited to `id + name`. Frozen and live names are compared only after removing a leading canonical number and separator, applying NFKC, and normalizing whitespace. The fixture requires all 18 Higher and 17 Lower S/4 HANA links to resolve uniquely, and replaces one unrelated row with a duplicate governed description to prove ambiguity remains unresolved and fail-closed. Numbered Controls continue to use only the canonical-number path.

The 0.2.83 targeted regression executes the real signed Operation catalog parser against `SAPECC.01`, `SAPS4HANA.01`, `SAP S/4 HANA.01`, `SAP_S4_HANA.01`, and `S4HANA.01`, requires each to resolve to the governed `SAP.01`, and proves `TOOL.05` is unchanged. It parses the managed workbook bytes and requires exact `REL.APP.SAP_S4_HANA.*` inventories of 18 Higher and 17 Lower links with `SAP.03` Higher-only. The Tool abnormal-path case proves dependency isolation does not survive into a fresh continuation and an absent exact association remains unverified for read-only reconcile. These are targeted source checks, not a live Pack mutation canary.

The current 0.2.94 / sequence 96 source acceptance inventory contains 30 independently addressed offline fixtures: 14 Python fixtures edit the real managed V5 OOXML template in memory and execute the production parser, revision validator and frozen plan compiler; 16 Worker/Operation fixtures cover the DAG, capability projection, failure isolation, registry, scoring, Risk-Control, APP settings/category directories, DCNO Network authority resolution and the recorded Higher catalogs. These fixtures run through the packager's `--self-check` path; packaging or installation does not claim a live Omnia Return canary.

The source inventory covers the real signed V3 copy without a user `isDataAvailable` column, canonical revisioned `GRA-` naming, exactly 11 check identities, parser-issue preservation, every-blocker check mapping, field limits/legal names, one-or-more APP inheritance for DB/OS/DCNO, one-or-more Tool relations, cross-Workspace warnings, off-batch exact current-Pack resolution, mixed-RAIT Higher derivation, five-kind create/reuse paths, exclusion revalidation, Run-scoped issue IDs, and atomic Core CAS revision commits.

DCNO acceptance requires the managed V5 bytes to prove exact section recognition as `kind=DCNO`, required-field and `网络` enum enforcement, batch-wide identity uniqueness, one-or-more relationship parsing, cross-Workspace warning behavior, off-batch exact live resolution, mixed-RAIT Higher derivation, and missing/ambiguous/non-APP/type rejection cases. Review must expose the registry fields. The current registry fixture requires `returnSupport=supported`, Higher/Lower Risk-Control support, the parameterized stages `object/relation/gra/inherited_rait/risk_classification/risk_control/evaluation`, `InfrastructureApplication`/602 relation policy, any-Higher-else-Lower inheritance, and true capabilities for object/GRA/relation/inherited RAIT/Risk-Control/Evaluation while APP-only settings/direct RAIT/scoring/AI review remain false. No test may treat an OS request as DCNO evidence.

DCNO negative UI coverage must prove that the APP-only `IT风险评估` category/Factors path is never reported as a DCNO error and no category/scoring target is generated. The immutable recording is Artifact `110eba6d-dd39-4b20-bfd5-83caefd20260` / recordingId `8aa3673e-53b7-4902-bca6-7b86d5cc62be` with 992 events; its exact catalog identities are reused across modes. Higher links are `RAITCOR008→DCNO.05/.21` and `RAITCOR006→DCNO.10`; Lower links are `RAITCOR008→DCNO.05` and `RAITCOR006→DCNO.10`; `RAITCOR001` is explicitly `ClassificationNA` in both modes.

Mixed DB/OS/DCNO inheritance coverage must assert the structured `inheritanceDecision` source list, `any_higher_else_all_lower` policy and Higher result, while `infrastructure_rait` remains passed with the exact Higher-priority explanation. Missing/invalid APP RAIT and failed identity/type/Workspace evidence remain blocking. Recording/read-only catalog evidence and targeted offline checks do not establish a real Omnia canary; no DCNO live Return canary is claimed by this candidate.

All 15 non-empty APP/DB/OS/Tool combinations remain parseable and the current DCNO Higher fixture exercises the fifth kind's signed Return orchestration. New APP freezes signed `isDataAvailable=false`; a pre-existing APP preserves only its authoritative boolean. The execution contract requires bounded identity pagination, no permit from diagnostic reads, an object-type-aware create-only permit, relation-before-GRA for DB/OS/DCNO, live APP GRA RAIT inheritance, read-back for every mutation, and read-only uncertain reconciliation. These source expectations and the recorded DCNO evidence are not a claim that a real Pack canary passed.

LibreOffice render evidence is a separate visual QA artifact. No automated fixture counts as a real Omnia canary.

The package carries declared test inventory and vectors, not a fabricated pass result. Actual command results belong to the repository acceptance record. Automated Connector harness results are not a real Omnia canary.

Version 0.2.68 requires a real Pack canary proving that each APP creates exactly one category target, the signed preflight resolves one live `riskFactorGrouping` with the exact Chinese name, PATCH carries a live concurrency test and `/applicable=true`, and signed GET read-back observes the same category as enabled before scoring/evaluation. A mixed APP/DB/OS/Tool run must prove DB/OS/Tool issue no category Operation. A category identity/concurrency/read-back failure must leave the target non-successful, preserve receipt/reconcile evidence, isolate only the affected row when confirmed, and never allow that row's Risk Factors or Evaluation to run. Source inspection and package generation are not this canary.

The same canary must cover at least SAP ECC, Generic Application, and S/4 HANA in both Higher and Lower modes. Evidence must show every APP consumes the same 15 APP scoring identities while selecting only its own Control family, category `verified=true` precedes the first factor preflight, every live factor matches governed display order plus label, and each applicable factor reads back its requested live spectrum extreme. S/4 HANA cannot be marked passed until the connector canary exists; source code alone is not evidence.

The S/4 canary must also prove the user template and Review selector expose the exact label `SAP S/4 HANA`, the parsed Run persists that label in `appContentAllowedValues`, and the live Omnia content resolves to the `SAP_S4_HANA` scoring/relation family. Negative packaging or preparation evidence must remove one S/4 scoring or relation family and observe a blocking governance error before mutation. Regression evidence must keep Generic, SAP ECC, DB, OS, and Tool validation/Return behavior unchanged.

The current source scheduling change requires a real Pack regression with at least one APP and multiple dependent DB/OS rows. Evidence must show: the APP core completes before dependent infrastructure starts; no more than three row branches are active by default; every command still has ordered preflight/commit/read-back evidence; a row's post-evaluation command starts only after that row's own EvaluationComplete receipt; one branch failure stops new dispatch while in-flight branches finish their receipt chain; and the Run is finalized once with `uncertain > failed > succeeded`. Source syntax/build evidence does not satisfy this canary requirement.

Version 0.2.54 requires a real Pack regression covering every APP/DB/OS/Tool subtype, Generic and SAP ECC APPs in Higher and Lower modes, DB/OS single- and multi-APP sources, any-Higher and all-Lower derivation, and both Tool subtypes with exact APP relations. Evidence must prove Settings/post-settings overlap, exact Tool Risk-Control read-back, real relation creation/reuse, monotonic progress and durable item isolation. Packaging or syntax checks alone are not a passed canary.

Version 0.2.50 additionally requires the fixed rail to expose both actions in every phase and the Core state matrix to remain authoritative: Upload back is disabled; Validate back preserves Run/Artifact/revisions; waiting confirmation back invalidates the unconsumed token/intents; stable restart opens a new upload while preserving retained audit; active validation/Return/reconcile/uncertain states reject both actions. Confirm/continue still retains the authorized mutation invocation until Return execution reaches a verified terminal, failed, or uncertain result. Generated Risk classification must use a unique durable field intent, exact immutable Core payload binding, a Risk-owned concurrency token when supplied by Omnia, receipt-backed readback and GRA projection before any governed Risk-Control association. The parent Assessment token must never be used as a Risk token. Per-target checkpoint persistence, strict managed-object continuation, live Core progress projection, and the 120-second/40-read settlement boundary remain required. Repository unit tests are intentionally not run for this change; release verification is limited to source syntax, production build and diff hygiene as requested.
